requests >= 2.25.0
paramiko >= 2.11.0
httpx >= 0.23.0
PySide6 >= 6.7.0
feedparser >= 6.0
readability-lxml >= 0.8
html2text >= 2020.1.16
pytrends >= 4.9.0
python-dotenv >= 1.0.0

# OCR plugin — optional dependencies (uncomment what you need)
# pdf2image >= 1.16.0      # LLM backend: PDF rasterisation (also needs poppler)
# pdfplumber >= 0.9.0      # pdf backend: text-layer PDF extraction
# python-docx >= 1.0.0     # docx backend
# openpyxl >= 3.1.0        # xlsx backend
# pytesseract >= 0.3.10    # pdf/image backend (also needs tesseract installed)
# Pillow >= 10.0.0         # image backend
